# Always install devDependencies, even when NODE_ENV=production.
# Required because we compile TypeScript during the production build on Render,
# which needs @types/* and `typescript` itself (both in devDependencies).
# Without this line, NODE_ENV=production causes npm install to skip devDeps.
production=false
